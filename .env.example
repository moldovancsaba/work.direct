# Set these in Vercel → Project settings → Environment Variables (Production). Never commit real values.
MCP_PATH_SECRET=        # openssl rand -hex 24
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=   # obtained once via /<MCP_PATH_SECRET>/oauth/start
